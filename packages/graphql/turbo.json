{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "../../apps/homepage/src/**/*.graphql",
        "../../apps/remote/src/**/*.graphql",
        "../../apps/renderer/src/**/*.graphql"
      ]
    }
  }
}