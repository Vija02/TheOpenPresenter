/**
 * App-wide config. Vega has no Expo-style EXPO_PUBLIC_* env vars,
 * so the server URL is embedded at compile time here.
 *
 * To change between environments, edit this file (or wire up
 * `babel-plugin-inline-dotenv` later).
 */

export const DEFAULT_ROOT_URL = 'https://theopenpresenter.com';
