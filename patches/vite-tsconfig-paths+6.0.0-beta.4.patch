diff --git a/node_modules/vite-tsconfig-paths/dist/index.js b/node_modules/vite-tsconfig-paths/dist/index.js
index e48befa..9251875 100644
--- a/node_modules/vite-tsconfig-paths/dist/index.js
+++ b/node_modules/vite-tsconfig-paths/dist/index.js
@@ -7,6 +7,10 @@ import { isAbsolute as isAbsolute2, join as join2, relative as relative2 } from
 import { inspect } from "util";
 import * as vite2 from "vite";
 
+const normalizePath = (path) => {
+  return path.slice(0, 1).toUpperCase() + path.slice(1).replace(/\\/g, '/')
+}
+
 // src/debug.ts
 import createDebug from "debug";
 var debug = createDebug("vite-tsconfig-paths");
@@ -45,7 +49,7 @@ import * as path from "path";
 import * as vite from "vite";
 var isWindows = os.platform() == "win32";
 var normalize = (p) => {
-  let output = vite.normalizePath(p);
+  let output = normalizePath(p);
   if (isWindows && output[1] === ":") {
     output = output[0].toUpperCase() + output.substring(1);
   }
