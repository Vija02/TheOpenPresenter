query ProjectMediaInfo($mediaIds: [UUID!]) {
  mediaImageSizes(filter: { imageMediaId: { in: $mediaIds } }) {
    nodes {
      imageMediaId
      processedMediaId
      width
      fileType
    }
  }
  mediaImageMetadata(filter: { imageMediaId: { in: $mediaIds } }) {
    nodes {
      imageMediaId
      width
      height
    }
  }
  mediaDependencies(
    filter: {
      or: [
        { parentMediaId: { in: $mediaIds } }
        { childMediaId: { in: $mediaIds } }
      ]
    }
  ) {
    nodes {
      parentMediaId
      childMediaId
    }
  }
  mediaVideoMetadata(
    filter: {
      or: [
        { videoMediaId: { in: $mediaIds } }
        { hlsMediaId: { in: $mediaIds } }
        { thumbnailMediaId: { in: $mediaIds } }
      ]
    }
  ) {
    nodes {
      videoMediaId
      hlsMediaId
      thumbnailMediaId
      duration
      transcodeStatus
    }
  }
  projectMedias(filter: { mediaId: { in: $mediaIds } }) {
    nodes {
      projectId
      mediaId
      pluginId
    }
  }
}
