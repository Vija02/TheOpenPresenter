mutation SyncCloudConnection($cloudConnectionId: UUID!, $forceResync: Boolean) {
  syncCloudConnection(input: { cloudConnectionId: $cloudConnectionId, forceResync: $forceResync }) {
    success
  }
}
