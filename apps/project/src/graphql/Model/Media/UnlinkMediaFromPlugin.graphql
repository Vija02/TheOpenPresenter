mutation UnlinkMediaFromPlugin(
  $pluginId: UUID!
  $mediaUUID: UUID
  $projectId: UUID
) {
  unlinkMediaFromPlugin(
    input: { pluginId: $pluginId, mediaUUID: $mediaUUID, projectId: $projectId }
  ) {
    success
  }
}
