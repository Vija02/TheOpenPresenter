query SearchPublicOrganizations(
  $search: String!
  $first: Int = 20
  $offset: Int = 0
) {
  organizationsPublicSearch(
    first: $first
    offset: $offset
    searchString: $search
  ) {
    nodes {
      id
      slug
      name
    }
    totalCount
  }
}
