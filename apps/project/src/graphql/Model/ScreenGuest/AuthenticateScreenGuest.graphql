mutation AuthenticateScreenGuest(
  $organizationId: UUID!
  $passcode: String!
) {
  authenticateScreenGuest(
    input: { organizationId: $organizationId, passcode: $passcode }
  ) {
    success
  }
}
