fragment Project on Project {
  id
  name
  slug
  isTemporary
  category {
    id
    name
  }
  projectTags {
    nodes {
      id
      tag {
        id
        ...Tag
      }
    }
  }
  cloudConnectionId
  cloudLastUpdated
  createdAt
  targetDate
  updatedAt
}
