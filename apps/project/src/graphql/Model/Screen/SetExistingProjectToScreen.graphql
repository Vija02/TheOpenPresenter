mutation SetExistingProjectToScreen($screenId: UUID!, $projectId: UUID) {
  setExistingProjectToScreen(
    input: { screenId: $screenId, projectId: $projectId }
  ) {
    screen {
      id
      ...Screen
    }
  }
}
