query OrganizationLatestIndexPage($slug: String!) {
  ...SharedOrganization
  organizationBySlug(slug: $slug) {
    id
    projects(orderBy: [TARGET_DATE_DESC, UPDATED_AT_DESC], first: 1) {
      nodes {
        id
        ...Project
      }
    }
  }
}
