query OrganizationScreensIndexPage($slug: String!) {
  ...SharedOrganization

  organizationBySlug(slug: $slug) {
    id
    screens(orderBy: [NAME_ASC]) {
      nodes {
        id
        ...Screen
      }
    }
  }
}
