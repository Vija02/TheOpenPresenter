query OrganizationDashboardHostProjects($projectIds: [UUID!]) {
  projects(filter: { id: { in: $projectIds } }) {
    nodes {
      id
      ...Project
      organization {
        id
        slug
      }
    }
  }
}
