mutation ForgotPassword($email: String!, $turnstileToken: String) {
  forgotPassword(input: { email: $email, turnstileToken: $turnstileToken }) {
    # This mutation does not return any meaningful result,
    # but we still need to request _something_...
    clientMutationId
  }
}
