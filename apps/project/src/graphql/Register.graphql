mutation Register(
  $password: String!
  $email: String!
  $name: String
  $turnstileToken: String
) {
  register(
    input: {
      password: $password
      email: $email
      name: $name
      turnstileToken: $turnstileToken
    }
  ) {
    user {
      id
      username
      name
    }
  }
}
