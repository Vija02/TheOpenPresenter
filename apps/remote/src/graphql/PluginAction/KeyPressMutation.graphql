mutation KeyPress(
  $keyType: String!
  $projectId: String!
  $rendererId: String!
) {
  pluginKeyPress(
    input: { keyType: $keyType, projectId: $projectId, rendererId: $rendererId }
  ) {
    success
  }
}
